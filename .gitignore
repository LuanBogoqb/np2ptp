/target
**/*.rs.bk

# Local node state created by running the CLI (stores, downloaded output)
.np2ptp-store/
download/
*.nptp

# Generated research reports (regenerate with `cargo run -p np2ptp-sim`)
/reports/
# (Cargo.lock is committed — this workspace ships a binary)

# Subagent-driven-development scratch (task briefs, reports, review diffs, ledgers)
.superpowers/
